import React from "react";
import styles from "./footer.module.css";

const Footer = (props) => {
  return (
    <footer className={styles.footer}>
      <p className={styles.title}>당신의 하루는 어떤가요?</p>
    </footer>
  );
};

export default Footer;
